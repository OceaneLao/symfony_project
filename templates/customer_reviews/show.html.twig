{% extends 'base.html.twig' %}

{% block title %}CustomerReviews{% endblock %}

{% block body %}
    <h1>CustomerReviews</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer_review.id }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ customer_review.picture }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ customer_review.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ customer_review.lastname }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ customer_review.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_customer_reviews_index') }}">back to list</a>

    <a href="{{ path('app_customer_reviews_edit', {'id': customer_review.id}) }}">edit</a>

    {{ include('customer_reviews/_delete_form.html.twig') }}
{% endblock %}
